# .agent/skills/greeting.skill.yaml
aes_skill: "1.3"

id: "greeting"
name: "Greeting"
version: "1.0.0"
description: "Greet users warmly and contextually when they start a conversation or say hello."
activation: "auto"
emoji: "\U0001F44B"

stage: 1
phase: "interaction"

inputs:
  required: []
  optional: []
  environment: []

outputs: []
prerequisites: []

triggers:
  - type: "manual"
    command: ""

error_handling:
  strategy: "skip-on-error"

tags:
  - "conversation"
