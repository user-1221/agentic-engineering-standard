# {{ name }} — Operations Memory

> This file tracks activity across all workflow commands.
> Each command has its own section with checkmarked history.
> The agent reads this before starting any command to understand previous activity.
{% for cmd in workflow_commands %}

---

## {{ cmd.trigger }}

> **Worker:** {{ cmd.worker_specialty or cmd.description }}

**Last run:** Never
**Status:** Not started
**Phases:** {{ cmd.runbook_phases | map(attribute='title') | join(', ') }}

### Activity Log

_No activity recorded yet._

<!-- Format:
- [x] YYYY-MM-DD: Phase completed — what was done, outcome
- [ ] YYYY-MM-DD: Phase in progress — current state
- [ ] Pending — what still needs to be done
-->

### Issues & Decisions

_None._
{% endfor %}
